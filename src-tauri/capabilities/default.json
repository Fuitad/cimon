{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main Settings window. Update checks and install actions go through custom Rust commands, so no direct updater plugin webview permission is required.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "notification:default"
  ]
}
