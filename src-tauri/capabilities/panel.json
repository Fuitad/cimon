{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "panel",
  "description": "Capability for the tray popover panel window. It invokes app commands (status fetch, update checks/actions, open URL, show settings, quit, resize) and listens for app events; URL opening, updates, and quitting are handled in Rust, so the panel itself needs only the core defaults.",
  "windows": ["panel"],
  "permissions": ["core:default"]
}
